[스프링 핵심 원리 기본편 - 섹션3. 스프링 핵심 원리 이해1 - 예제 만들기](https://velog.io/@sehyun56/%EC%8A%A4%ED%94%84%EB%A7%81-%ED%95%B5%EC%8B%AC-%EC%9B%90%EB%A6%AC-%EA%B8%B0%EB%B3%B8%ED%8E%B8-%EC%84%B9%EC%85%983.-%EC%8A%A4%ED%94%84%EB%A7%81-%ED%95%B5%EC%8B%AC-%EC%9B%90%EB%A6%AC-%EC%9D%B4%ED%95%B41-%EC%98%88%EC%A0%9C-%EB%A7%8C%EB%93%A4%EA%B8%B0)

### 💡 프로젝트 생성

- https://start.spring.io/
- 역할과 구현을 분리해서 자유롭게 구현 객체를 조립할 수 있게 했기 때문에 회원 저장소는 물론, 할인 정책도 유연하게 변경 가능
- 객체 다이어그램은 클래스 다이어그램과 달리 실제 동적으로 객체들의 연관관계가 맺어지는 그림
- command + n : generate 단축키

---

### 💡 비지니스 요구사항과 설계

- 회원
    - 회원을 가입하고 조회 가능 → join(), findMember()
    - 회원은 일반과 VIP 두가지 등급 있음 → ENUM
- 주문과 할인 정책
    - 회원은 상품을 주문할 수 있음
    - 회원 등급에 다라 할인 정책을 적용할 수 있음
    - 할인 정책은 모든 VIP는 1000원을 할인해주는 고정 금액 할인을 적용(나중에 변경 가능)

→ 요구사항에 따르면 정책이 정확히 결정되지 않은 것이 많음 그러므로 인터페이스를 만들고 구현체를 나중에라도 바꾸게끔 설계하면 됨

---

### 💡 회원 도메인 설계, 개발, 실행 및 테스트

- 회원 객체 다이어그램
    - 클라이언트 → 회원 서비스 → 메모리 회원 저장소
- 회원 엔티티
    - 회원 등급은 enum 으로 따로 관리

        ```java
        public enum Grade {
            BASIC,
            VIP
        }
        ```

    - 회원 엔티티
        - id, name, grade 필드
        - 생성자
        - getter, setter
- 회원 저장소
    - 회원 저장소 인터페이스

        ```java
        public interface MemberRepository {
            void save(Member member);
            Member findById(Long memberId);
        }
        ```

    - 메모리 회원 저장소 구현체
        - 데이터베이스가 아직 확정이 안 났으므로 일단 메모리 회원 저장소를 구현해서 개발 진행

        ```java
        public class MemoryMemberRepository implements MemberRepository{
            private static Map<Long, Member> store = new HashMap<>();
            @Override
            public void save(Member member) {
                store.put(member.getId(), member);
            }
        
            @Override
            public Member findById(Long memberId) {
                return store.get(memberId);
            }
        }
        ```

- 회원 서비스
    - 회원 서비스 인터페이스

        ```java
        public interface MemberService {
            void join(Member member);
            Member findMember(Long memberId);
        }
        ```

    - 회원 서비스 구현체

        ```java
        public class MemberServiceImpl implements MemberService{
            private final MemberRepository memberRepository = new MemoryMemberRepository();
            @Override
            public void join(Member member) {
                memberRepository.save(member);
            }
        
            @Override
            public Member findMember(Long memberId) {
                return memberRepository.findById(memberId);
            }
        }
        ```

- 회원 가입 main ( 회원 가입 실행 )
    - 애플리케이션 로직으로 이렇게 테스트하는 것보다 테스트 사용하는 것이 좋음

    ```java
    public class MemberApp {
        public static void main(String[] args) {
            MemberService memberService = new MemberServiceImpl();
            Member member = new Member(1L, "memberA", Grade.VIP);
            memberService.join(member);
    
            Member findMember = memberService.findMember(1L);
            System.out.println("new member = "+member.getName());
            System.out.println("find Member = "+findMember.getName());
        }
    }
    ```

- 회원 가입 테스트
    - @Test 어노테이션 붙여야 함
    - given, when, then
    - Assertions : import org.assertj.core.api.Assertions;

    ```java
    public class MemberServiceTest {
        MemberService memberService = new MemberServiceImpl();
        @Test
        void join() {
            // given
            Member member = new Member(1L,"memberA",Grade.VIP);
            // when
            memberService.join(member);
            Member findMember = memberService.findMember(1L);
            // then
            Assertions.assertThat(member).isEqualTo(findMember);
        }
    }
    ```


---

### 💡 주문과 할인 도메인 설계, 개발, 실행 및 테스트

- 주문 도메인 협력, 역할, 책임
    - 주문 생성 : 클라이언트는 주문 서비스에 주문 생성 요청
    - 회원 조회 : 할인을 위해서는 회원 등급이 필요하므로 회원 저장소에서 회원을 조회
    - 할인 적용 : 회원 등급에 따른 할인 여부를 할인 정책에 위임함
    - 주문 결과 반환 : 주문 서비스는 할인 결과를 포함한 주문 결과를 반환함
- 할인 정책
    - VIP면 1000원 할인, 아니면 할인 없음
    - 할인 정책 인터페이스

        ```java
        public interface DiscountPolicy {
            int discount(Member member, int price);
        }
        ```

    - 할인 정책 구현체

        ```java
        public class FixDiscountPolicy implements DiscountPolicy{
        
            private int discountFixAmount = 1000; // 1000원 할인
            @Override
            public int discount(Member member, int price) {
                if(member.getGrade() == Grade.VIP)
                    return discountFixAmount;
                else
                    return 0;
            }
        }
        ```

- 주문 엔티티
    - memberId, itemName, itemPrice, discountPrice 필드
    - 생성자
    - calculatePrice() : itemPrice - discountPrice
    - getter, setter
    - toString() 오버라이드하면 나중에 order 출력하면 toString에서 구현한대로 출력됨
- 주문 서비스
    - 주문 서비스 인터페이스

        ```java
        public interface OrderService {
            Order createOrder(Long memberId, String itemName, int itemPrice);
        }
        ```

    - 주문 서비스 구현체

        ```java
        public class OrderServiceImpl implements OrderService{
            private final MemberRepository memberRepository = new MemoryMemberRepository();
            private final DiscountPolicy discountPolicy = new FixDiscountPolicy();
            @Override
            public Order createOrder(Long memberId, String itemName, int itemPrice) {
                // '단일 책임 원칙'을 잘 지킨 코드
                Member member = memberRepository.findById(memberId);
                int discountPrice = discountPolicy.discount(member, itemPrice);
                return new Order(memberId,itemName,itemPrice,discountPrice);
            }
        }
        ```

- 주문과 할인 정책 실행

    ```java
    public class OrderApp {
        public static void main(String[] args) {
            MemberService memberService = new MemberServiceImpl();
            OrderService orderService = new OrderServiceImpl();
    
            Long memberId = 1L;
            Member member = new Member(memberId, "memberA", Grade.VIP);
            memberService.join(member);
    
            Order order = orderService.createOrder(memberId, "itemA", 10000);
    
            System.out.println("order = "+order);
        }
    }
    -> 결과
    order = Order{memberId=1, itemName='itemA', itemPrice=10000, discountPrice=1000}
    ```

- 주문과 할인 정책 테스트

    ```java
    public class OrderServiceTest {
        MemberService memberService = new MemberServiceImpl();
        OrderService orderService = new OrderServiceImpl();
    
        @Test
        void createOrder() {
            Long memberId = 1L;
            Member member = new Member(memberId, "memberA", Grade.VIP);
            memberService.join(member);
    
            Order order = orderService.createOrder(memberId,"itemA",10000);
            Assertions.assertThat(order.getDiscountPrice()).isEqualTo(1000);
        }
    }
    ```


---

### 💡 새롭게 알게 된 점 및 중요한 점 정리

1. 인터페이스를 사용함으로써 역할과 구현을 분리해서 자유롭게 구현 객체를 조립할 수 있게 설계함
    1. 추후 변경이 필요할 때도 유연하게 대응 가능
    2. 인터페이스를 짜고 인터페이스를 구현하는(implements) 구현체 코드 짜기
2. 테스트
    1. @Test 어노테이션 붙이기
    2. given, when, then 으로 코드 짜기
    3. Assertions 사용
3. enum 사용
    1. Member member = new Member(1L, "memberA", Grade.VIP);
    2. enum 클래스(열거 타입)

    ```java
    public enum Grade {
        BASIC,
        VIP
    }
    ```

4. 순서
    1. 엔티티 → 저장소 → 서비스 → 실행 순으로 개발 진행
    2. 각 단계에서 이전 단계의 객체를 사용해야 하므로, member 실행 코드에서 인스턴스를 생성하여 활용
5. 클래스 내부에 private final
    1. private : 클래스의 필드(멤버 변수)에 private을 붙여 외부 접근을 막고, 캡슐화 유지
    2. final :
    - 객체가 생성될 때 한 번만 의존성을 주입하고, 이후 변경되지 않도록 보장
    - 중간에 바뀌지 않으므로 예상치 못한 변경을 방지할 수 있음
    - 안 붙이면 다른 객체로 변경할 수 있으므로 실수로 다른 곳에 할당할 수도 있음 → 예기치 않은 버그 발생 가능성 증가